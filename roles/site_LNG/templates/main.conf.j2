{# this generates MDF specific config. It will be merged with the config generated by the common role before it is applied to the MDF #}

# *** site_LNG CONFIGUATION ***

{# SET HELPERS #}
city: {{ location.city }}
state: {{ location.state }}
